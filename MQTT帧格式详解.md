# MQTT 协议帧格式详解（基于本项目 · 华为云 IoTDA 接入）

> 对应固件：`kuan/System/mqtt.c` / `mqtt.h` / `esp8266.c`，全部字节长度按本项目真实字符串核算。
> 项目接入参数：设备名 `6a83c960cbb0cf6bb97a572f_tdd123`（31 字符），ClientID = 设备名 + `_0_0_2026081803`（46 字符），密码 64 位十六进制（64 字符），KeepAlive = 100 秒。

---

## 0. 万物皆一的结构：每个 MQTT 帧的公共骨架

所有 MQTT 报文都长这样：

```
┌──────────────────┬─────────────────┬──────────┬──────────┐
│ 固定报头(2~5字节) │ 剩余长度(1~4字节)│ 可变报头  │  载荷    │
└──────────────────┴─────────────────┴──────────┴──────────┘
```

### 0.1 固定报头第 1 字节：高 4 位是"身份"，低 4 位是"标志"

| 高4位(类型) | 低4位(标志) | 例子 |
|---|---|---|
| 0001=CONNECT | 0000 | `0x10` |
| 0011=PUBLISH | 0000(QoS0) | `0x30`；若 QoS1 则 `0x32` |
| 1000=SUBSCRIBE | 0010(协议规定写死) | `0x82` |
| 1100=PINGREQ | 0000 | `0xC0` |

### 0.2 剩余长度：变长编码（唯一需要算的规则）

每 7 个二进制位装一段数字，最高位是"后面还有下一组"的旗子：

- 长度 ≤127 → 1 字节搞定。如 66 = `0x42`
- 长度 237 → 237÷128 余 109，第一字节 = 109+旗子 = `0xED`，商 1 = 第二字节 `0x01` → `0xED 0x01`
- 理论最多 4 字节可表示 256MB，足够任何报文

### 0.3 字符串的通用规矩

凡是报文里出现字符串（协议名、主题、ClientID……），**前面都垫 2 个字节的长度**（大端序，高字节在前）。云靠它精确切分。

---

## 1. 一次完整会话的时序全景

```
STM32                                华为云(1883端口)
  │──① CONNECT (0x10) 登门自我介绍──────▶│
  │◀──② CONNACK (0x20 0x02) "请进"──────│
  │──③ SUBSCRIBE (0x82) 留呼机号────────▶│
  │◀──④ SUBACK (0x90…) "呼机号登记成功"──│
  │──⑤ PUBLISH (0x30) 每5s发数据────────▶│   (QoS0 无回执)
  │──⑥ PINGREQ (0xC0 00) 每30s保活──────▶│
  │◀──⑦ PINGRESP (0xD0 00) "在"─────────│
  │──⑧ DISCONNECT (0xE0 00) 辞职信──────▶│  (本项目未调用)
```

---

## 2. CONNECT（0x10）——登录帧逐段拆解

函数：`MQTT_Connect()`。总长 159 字节。

| 段 | 字节 | 值 | 大白话 |
|---|---|---|---|
| ① 类型 | 1 | `0x10` | 类型1=CONNECT |
| ② 剩余长度 | 2 | `0x9D 0x01` | 后面共 157 字节 = 10(可变报头) + 48(ClientID) + 33(用户名) + 66(密码) |
| ③ 协议名长度 | 2 | `0x00 0x04` | "接下来 4 字节是协议名" |
| ④ 协议名 | 4 | `4D 51 54 54` = "MQTT" | 固定写死 |
| ⑤ 协议等级 | 1 | `0x04` | MQTT 3.1.1 |
| ⑥ 连接标志 | 1 | `0xC2` = 1100 0010 | 位1=要用户名✓ 位2=要密码✓ 位7=清除旧会话✓（0xC2 是华为云设备接入的标准组合） |
| ⑦ 心跳周期 | 2 | `0x00 0x64` | KeepAlive = 100 秒；100s 内无任何来往，云判掉线 |
| ⑧ ClientID 长度 | 2 | `0x00 0x2E` | 46 |
| ⑨ ClientID | 46 | `36 61 38 33…` | 设备唯一身份证（云按它计连接数：**同 ID 只许一条连接**，冒充登录会互踢） |
| ⑩ 用户名长度+值 | 2+31 | `0x00 0x1F` + 设备名 | 华为云约定用户名=设备名 |
| ⑪ 密码长度+值 | 2+64 | `0x00 0x40` + 密钥 | 一机一密的 SHA256 摘要 |

完整字节流（16 进制示意）：

```
10 9D 01
00 04 4D 51 54 54            ← "MQTT"
04 C2 00 64
00 2E [46字节 ClientID]
00 1F [31字节 设备名]
00 40 [64字节 密钥]
```

## 3. CONNACK（0x20 0x02 0x00 0x00）——云的登录回执

固定 4 字节：`0x20`(类型2) + `0x02`(剩余长度2) + 会话标志 + **返回码**。

| 返回码 | 含义 |
|---|---|
| 0x00 | 连接成功 |
| 0x01 | 协议版本不对 |
| 0x02 | ClientID 不合法 |
| 0x04 | 用户名/密码错误 |
| 0x05 | 未授权 |

固件判断：`recv_buf[0]==0x20 && recv_buf[1]==0x02` → 成功。**被拒时查的顺序**：三元组是否抄错 → 密码是否过期 → 是否被另一条同名连接挤掉。

## 4. SUBSCRIBE（0x82）——订阅帧逐段拆解

函数：`MQTT_SubscribeTopic()`。总长 68 字节。

| 段 | 字节 | 值 | 大白话 |
|---|---|---|---|
| ① 类型 | 1 | `0x82` | 类型8=SUBSCRIBE；低4位 `0010` 是协议规定写死的 |
| ② 剩余长度 | 1 | `0x42` | 66 = 2(报文ID) + 2+61(主题) + 1(QoS) |
| ③ 报文 ID | 2 | `0x00 0x01` | 给这封信用个编号，SUBACK 会带回来对账 |
| ④ 主题长度 | 2 | `0x00 0x3D` | 61 |
| ⑤ 主题 | 61 | `$oc/devices/6a83…tdd123/sys/messages/down` | "云下发消息的信箱"——留了这个号，云端命令才会找到我 |
| ⑥ QoS 等级 | 1 | `0x00` | 这个主题用 QoS0 收 |

完整字节流：

```
82 42
00 01
00 3D 24 6F 63 2F …  ← "$oc/devices/…" 61字节
00
```

> 备注：`mqtt.h` 里还定义了 `…/sys/commands/request_id={request_id}`（命令主题，80 字符），只在未调用的退订函数里用到；固件实际订阅的是 `messages/down`。两条主题里 `{request_id}` 是占位符，云下发命令时会替换成真实 ID。

## 5. SUBACK（0x90 0x03 0x00 0x01 0x00）——订阅回执

固定 5 字节，逐段：

| 段 | 字节 | 含义 |
|---|---|---|
| 类型 | `0x90` | 类型9=SUBACK |
| 剩余长度 | `0x03` | 后面 3 字节 |
| 报文 ID | `0x00 0x01` | 呼应 SUBSCRIBE 里的编号（对账凭据） |
| 返回码 | `0x00`/`0x01` | 0x00/0x01=按 QoS0/1 订阅成功；**0x80=订阅被拒** |

固件判断（esp8266.c）：`recv_buf[0]==0x90 && recv_buf[1]==0x03 && recv_buf[2]==0x00 && recv_buf[3]==0x01 && (recv_buf[4]==0x00 || recv_buf[4]==0x01)`。

## 6. PUBLISH（0x30）——数据帧逐段拆解（每 5s 一帧的主角）

函数：`MQTT_PublicTopic()`。以一帧真实数据（temp=29.3 / humi=18.0 / gas=6.3 / alarm=4）为例，总长 239 字节。

```
┌──────┬───────┬───────┬───────────────────────────────┬──────────────┐
│ 0x30 │ ED 01 │ 00 41 │ 65字节主题                     │ 170字节JSON  │
│ 类型  │剩余长度│主题长度│ 收件地址                       │ 信纸          │
└──────┴───────┴───────┴───────────────────────────────┴──────────────┘
```

| 段 | 字节 | 值 | 大白话 |
|---|---|---|---|
| ① 类型 | 1 | `0x30` | 类型3=PUBLISH；QoS0：发完不等回执 |
| ② 剩余长度 | 2 | `0xED 0x01` | 后面共 237 = 2+65+170（变长编码两字节实例） |
| ③ 主题长度 | 2 | `0x00 0x41` | 65 |
| ④ 主题 | 65 | 见下 | 收件地址，四段各有含义 |
| ⑤ 载荷 | 170 | 见下 | 信纸 |

**④ 主题四段拆**：`$oc`（华为云系统保留前缀）/ `devices/6a83…tdd123`（哪台设备，云靠它归档）/ `sys`（系统级主题）/ `properties/report`（动作=属性上报）。

**⑤ 载荷 170 字节逐小段**：

```
{"services":[                                  ← 华为云强制外壳：服务数组
  {"service_id":"show",                        ← 服务名：与控制台产品模型一字不差
   "properties":{                              ← 属性集合
     "humi":18.000000,  "temp":29.300000,     ← 与蓝牙帧相同的8字段
     "gas":6.300000,    "fan":0,
     "alarm":4,         "helmet":0, "head":2
   },
   "event_time":"20151212T121212Z"             ← 时间戳占位(设备无可靠时钟，
  }] }                                           云按消息到达时间入库)
```

细节与考点：
- `18.000000` 的 6 位小数是 C 语言 `%f` 默认格式，JSON 数值无影响；
- 华为云**只存产品模型里登记过的属性**——helmet/head 就吃过"固件先发、云端没登记被默默丢弃"的亏；
- **QoS0 的 PUBLISH 没有报文 ID 段**；若 QoS1（首字节 `0x32`）则紧跟剩余长度多出 2 字节报文 ID，且云要回 PUBACK（0x40）。我们选 QoS0：监控数据 5s 一帧，丢一帧下一帧就覆盖，不值得付出往返开销。
- 完整性没有应用层 CRC：无线段 BLE/TCP 各自链路层有校验，坏帧靠"下一周期自然覆盖"兜底。

## 7. 云→设备的下行 PUBLISH（命令怎么来）

结构和上行完全一样，方向反过来：首字节 `0x30`，主题是你订阅过的 `…/sys/messages/down`（或 commands 主题，云会把 `{request_id}` 替换成真实 ID），载荷是命令 JSON。固件 `MQTT_RX_DATE_DEAL()` 在串口中断里对载荷做关键字解析（如查 `"LED"` 并执行开关）。

## 8. PINGREQ / PINGRESP——保活对

```
板子→云：C0 00    （类型12 PINGREQ，剩余长度0——全协议最短的帧）
云→板子：D0 00    （类型13 PINGRESP）
```

KeepAlive=100s 内若没有任何其他报文，必须发 PINGREQ，否则云断开连接。本项目 report 任务**每 30 秒**（约 6 次上报周期）调 `MQTT_SendHeart()` 发探活，`ESP8266_MQTT_PingCheck()` 等待 2s 内出现 `0xD0`；等不到判定链路断，重走 `ESP8266_Init()`（AT+RST → 连 WiFi → TCP → CONNECT → SUBSCRIBE）全量自愈。

## 9. DISCONNECT（0xE0 00）——辞职信

2 字节，礼貌离场。固件 `MQTT_Disconnect()` 已实现但未调用——设备断电属于不辞而别，云靠心跳超时自行发现。

---

## 10. 总表：八种报文一张卡记住

| 报文 | 首字节 | 方向 | 时机 | 固件函数 | 本项目判断处 |
|---|---|---|---|---|---|
| CONNECT | 0x10 | →云 | 开机一次 | MQTT_Connect | — |
| CONNACK | 0x20 | ←云 | 登录回执 | — | esp8266.c 比对 0x20 0x02 |
| SUBSCRIBE | 0x82 | →云 | 登录后 | MQTT_SubscribeTopic | — |
| SUBACK | 0x90 | ←云 | 订阅回执 | — | esp8266.c 比对 5 字节 |
| PUBLISH | 0x30 | →云 | **每5s+告警补发** | MQTT_PublicTopic | — |
| 下行 PUBLISH | 0x30 | ←云 | 云下发命令 | — | MQTT_RX_DATE_DEAL |
| PINGREQ | 0xC0 | →云 | 每30s | MQTT_SendHeart | — |
| PINGRESP | 0xD0 | ←云 | 保活回执 | — | ESP8266_MQTT_PingCheck 比对 0xD0 |
| DISCONNECT | 0xE0 | →云 | （未使用） | MQTT_Disconnect | — |

## 11. 高频追问速答

- **为什么不用现成 MQTT 库？** 裸机资源紧张，报文种类少且固定（CONNECT/SUBSCRIBE/PUBLISH/PINGREQ 四种），手拼二进制约 200 行、零动态内存，还逼自己吃透了协议——剩余长度变长编码、字符串前垫 2 字节长度这些细节都是手写才会真正理解。
- **为什么 QoS0？** 监控数据周期性重发，丢一帧 5s 后自然覆盖；指令走 BLE 通道且有 ack 机制。QoS1 的回执往返与重发去重逻辑对单片机是不必要的复杂度。
- **剩余长度为什么变长？** 小报文占 1 字节、大报文最多 4 字节（可表 256MB），在"省字节"和"表达范围"之间折中，是协议设计里典型的变长整数（VarInt）手法。
- **CONNACK 被拒怎么排？** 返回码 0x01 查协议等级 → 0x02 查 ClientID → 0x04 查用户名密码 → 0x05 查设备是否被禁用；另注意同 ID 双连接互踢造成的"假掉线"。
